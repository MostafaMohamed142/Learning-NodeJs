// modules are blocks of encapsulated code that communicate with an external application based on their functionality.

// modules are three types 1:core module , 2:local modules ,3: third-party modules

// 1- core modules : they are built in modules in nodejs which comes with the installation they can be loaded into the program with the require function
// const module = require('moduleName') => it returns a javascript type depending on what the particular module returns

// core modules are 1-HTTP {Creates an http server in nodejs},2:assert{set of assertion functions useful for testing} ,3:fs {used to handle filesystem} , 4:path {inculdes methods to deal with file path} , 5:procress {provides info and control about the current nodesjs process} ,6:os {provides info about the operating system} , 7:querystring {utility used for parsing and formatting url query strings} 8:url {module provides utilties for url resolution and parsing}

//2-local modules : they are created locally in the node application

// 3- third party modules : modules available online form the npm




